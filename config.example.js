// --- API key setup (read this before you run the app) ---
//
// 1. Copy this file and name the copy config.js
// 2. Sign up at openweathermap.org and create a free API key
// 3. Paste your key below (replace the placeholder text)
// 4. config.js stays on your machine only — Git ignores it so your key is never pushed

const WEATHER_API_KEY = "your_openweathermap_api_key_here";
